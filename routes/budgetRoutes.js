const router=require('express').Router();const c=require('../controllers/budgetController');const {authenticate}=require('../middleware/auth');router.get('/',authenticate,c.list);router.get('/alerts',authenticate,c.alerts);router.post('/',authenticate,c.create);router.get('/:id',authenticate,c.get);router.put('/:id',authenticate,c.update);router.delete('/:id',authenticate,c.remove);module.exports=router;
