document.addEventListener('DOMContentLoaded',async()=>{try{const u=await api('/profile');const f=$('#profile-form');f.username.value=u.username;f.email.value=u.email}catch(e){toast(e.message)}$('#profile-form').addEventListener('submit',async e=>{e.preventDefault();try{const d=Object.fromEntries(new FormData(e.target));await api('/profile',{method:'PUT',body:JSON.stringify(d)});toast('Profile updated')}catch(err){toast(err.message)}});$('#password-form').addEventListener('submit',async e=>{e.preventDefault();try{const d=Object.fromEntries(new FormData(e.target));await api('/profile/password',{method:'PUT',body:JSON.stringify(d)});e.target.reset();toast('Password updated')}catch(err){toast(err.message)}})});
